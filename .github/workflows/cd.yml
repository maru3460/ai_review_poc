name: CD - Deploy & Smoke Test

# mainブランチへのプッシュでRailwayが自動デプロイされる。
# このワークフローはデプロイ後にヘルスチェックを実行してデプロイ成否を確認する。
on:
  push:
    branches: [main]

jobs:
  smoke-test:
    name: Smoke Test (post Railway deploy)
    runs-on: ubuntu-latest

    steps:
      - name: Backend health check
        # Railwayのデプロイ完了を待ちながら /health を最大10分ポーリングする
        env:
          BACKEND_URL: ${{ vars.RAILWAY_BACKEND_URL }}
        run: |
          echo "Waiting for Railway backend deployment..."
          if [ -z "$BACKEND_URL" ]; then
            echo "::error::RAILWAY_BACKEND_URL is not set. Add it to repository variables."
            exit 1
          fi
          BACKEND_URL="${BACKEND_URL%/}"
          if [[ "$BACKEND_URL" != http://* && "$BACKEND_URL" != https://* ]]; then
            BACKEND_URL="https://${BACKEND_URL}"
          fi
          HEALTH_URL="${BACKEND_URL}/health"
          for i in $(seq 1 20); do
            STATUS=$(curl -sS -L --max-redirs 5 -o /dev/null -w "%{http_code}" "$HEALTH_URL" 2>/dev/null || echo "000")
            if [ "$STATUS" = "200" ]; then
              echo "✅ Backend OK (attempt ${i}/20): HTTP ${STATUS}"
              exit 0
            fi
            echo "Attempt ${i}/20: HTTP ${STATUS} (${HEALTH_URL}), retrying in 30s..."
            sleep 30
          done
          echo "::error::Backend health check timed out after 10 minutes."
          exit 1

      - name: Frontend health check
        env:
          FRONTEND_URL: ${{ vars.RAILWAY_FRONTEND_URL }}
        run: |
          if [ -z "$FRONTEND_URL" ]; then
            echo "::error::RAILWAY_FRONTEND_URL is not set. Add it to repository variables."
            exit 1
          fi
          FRONTEND_URL="${FRONTEND_URL%/}"
          if [[ "$FRONTEND_URL" != http://* && "$FRONTEND_URL" != https://* ]]; then
            FRONTEND_URL="https://${FRONTEND_URL}"
          fi
          FRONTEND_HEALTH_URL="${FRONTEND_URL}/health"
          STATUS=$(curl -sS -L --max-redirs 5 -o /dev/null -w "%{http_code}" "$FRONTEND_HEALTH_URL" 2>/dev/null || echo "000")
          if [ "$STATUS" != "200" ]; then
            echo "::error::Frontend health check failed: HTTP ${STATUS} (${FRONTEND_HEALTH_URL})"
            exit 1
          fi
          echo "✅ Frontend OK: HTTP ${STATUS}"

      - name: Summary
        if: success()
        env:
          BACKEND_URL: ${{ vars.RAILWAY_BACKEND_URL }}
          FRONTEND_URL: ${{ vars.RAILWAY_FRONTEND_URL }}
        run: |
          echo "### ✅ スモークテスト通過" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| サービス | URL |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          echo "| Backend | ${BACKEND_URL}/health |" >> $GITHUB_STEP_SUMMARY
          echo "| Frontend | ${FRONTEND_URL}/health |" >> $GITHUB_STEP_SUMMARY
